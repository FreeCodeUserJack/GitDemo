this is sample proj
use git init in cmd in this dir
stage (commit lvl 1) - git add *
	git status to see changes
commit the files in the stage -> git commit -m "comment here"
git remote add origin https://github.com/FreeCodeUserJack/GitDemo.git
git push origin master  // master branch -u would set "upstream" central location

make a change, then go to dir in cmd

git clone link_to_repository // get "fresh" repository
git pull origin master // get updates after first clone

git checkout -b branch_name // create a new branch at the local dir
	git branch to see all branches and the curr branch
	after done, you can stage, commit, etc.
	git push origin develop // PUSH TO DEV BRANCH
git pull origin develop // to get develop branch
git checkout develop // witch branch
